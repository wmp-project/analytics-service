name: CI - Analytics Service
on: [push]
jobs:
  docker_build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v6
      - name: List files
        run: ls -l
      - name: Build Image
        run: make docker-build



